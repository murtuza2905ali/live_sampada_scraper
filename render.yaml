services:
  - type: web
    # …
    envVars:
      - key: ENV
        value: production
      - key: CHROME_BINARY
        value: /usr/bin/chromium
      - key: CHROMEDRIVER_PATH
        value: /usr/bin/chromium-driver
      - key: ALLOWED_HOSTS
        # include your Render domain; you can comma-separate multiple
        value: "live-sampada-scraper.onrender.com"
